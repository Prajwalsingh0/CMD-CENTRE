package com.aicommandcenter.document.dto;

/** The document operations the AI layer is allowed to perform. */
public enum DocumentInsightOperation {
    SUMMARY,
    KEY_POINTS,
    NOTES,
    QUESTIONS
}
